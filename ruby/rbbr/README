rbbr README
===========

  rbbr (RuBy BRowser) is an application to browse
  modules/classes hierarchy and their constants and methods. If
  the optionally modules to refer documents are installed, you
  can also refer the document of methods in the browser.

Requirements
------------

  * ruby 1.6 or later
  * Ruby/GTK2 or later
  * (option) ReFe 0.4.2 or later
             Iconv is also required.
  * (option) ri

Install
-------

  De-Compress archive and enter its top directory.
  Then type:

    $ ruby install.rb config
    $ ruby install.rb setup
   ($ su)
    # ruby install.rb install

  You can also install files in your favor directory by
  supplying install.rb some options. Try "ruby setup.rb --help".

Document Referer(Option)
------------------------

  These document referers are avilable from rbbr.
  You need install them too.

  * ri
    Install ri by its normal method.

  * ReFe(Japanese only)
    Install ReFe by its normal method.
    This option requires Iconv module(See RAA).

Usage
-----

  Run the following command:

    $ rbbr

License
-------

  This program is free software.
  You can distribute/modify this program under
  the terms of the Ruby Distribute License.

Author
------
  (Ruby-GNOME2 and later) Ruby-GNOME2 Project <http://ruby-gnome2.sourceforge.net>
  (Original Author)Hiroshi Igarashi <iga@ruby-lang.org>
